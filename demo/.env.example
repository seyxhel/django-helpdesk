# demo/.env.example
# Copy this file to demo/.env and fill in your SMTP settings (do not commit secrets).
# Example for Gmail SMTP with an App Password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=you@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
FROM_EMAIL=primedesk0@gmail.com
# Optionally override DEFAULT_PASSWORD_RESET_DOMAIN used in generate_reset_link.py
# DEFAULT_PASSWORD_RESET_DOMAIN=127.0.0.1:8000
